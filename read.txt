第一种方式：基于https协议：
仓库就是目录：
1.创建空目录，名称为shop（任取）
2.复制https对应得地址。
然后在当前目录新建shop目录

$ mkdir shop
$ cd ./shop
1
2
3.使用clone指令克隆线上仓库到本地。

$ git clone 线上仓库地址
1
执行后，线上的仓库就被clone下来了，如果clone下来的是一个空目录，克隆下来的目录只有隐藏的.git目录。

在long里面新增的条件
